= simple_form_for @account do |f|
  .form-group
    = f.input :name, label: t('account.name')
  .form-group
    = f.input :funds, label: t('account.initial_amount')
  .form-group
    = f.button :submit, t("common.create")
    = link_to t('common.cancel'), :back, class: "btn btn-default"

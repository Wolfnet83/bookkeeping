.col-md-4.col-md-offset-4
  - unless user_signed_in?
    %h3
      = t('user.should_login')
  - else
    %h3
      = t('hello')
      = current_user.email
    %p
      Welcome to your personal dashboard
